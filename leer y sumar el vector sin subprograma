

#include "stdafx.h"

#define NMAX 100
int main(void)
{ int N, A[NMAX],i, S;
    S=0;
   printf("\n Tamaño del vector:");
   scanf("%d",&N);
   for(i=0;i<N;i++) // lee el vector
   {scanf("%d",&A[i]);
    }
   for(i=0;i<N;i++)
   {S=S+A[i];
   }
   printf("\n SUMA= %d \n",S);
 return 0;
}

