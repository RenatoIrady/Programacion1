
#include "stdafx.h"
#define NMAX 100

void MULT_VEC(int*A, int N);

int main(void)
{int N, A[NMAX];
  scanf(" %d ",&N);
  MULT_VEC(A,N);

return 0;
}
 
void MULT_VEC(int*A, int N)
{ int i, j,B;
  j=0;
  scanf("%d", &B);
  for(i=0;i<N;i++)
  { j= j+B;
    A[i]= j;
	printf("\n %d", A[i]);
  }
}
